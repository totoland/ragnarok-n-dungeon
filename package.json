{
  "name": "dungion-ro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "3D belt-scrolling dungeon brawler (Dungeon & Fighter style) with Ragnarok heroes. Three.js, no build step.",
  "scripts": {
    "dev": "node serve.mjs",
    "test": "node --test --import ./tests/setup-three.mjs tests/*.test.js",
    "sim": "node tools/playtest.mjs",
    "export": "sh tools/export_heroes.sh"
  }
}
